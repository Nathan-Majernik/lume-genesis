# conda env create -f environment.yml
name: lume-genesis-dev
channels:
  - conda-forge
dependencies:
  - python>=3.10
  # Core python dependencies
  - lark
  - lume-base
  - matplotlib
  - numpy
  - openpmd-beamphysics >=0.14.1
  - ormsgpack
  - prettytable
  - psutil
  - pydantic >=2.12.0
  - pydantic-settings
  - typing-extensions
  # Genesis - OpenMPI variants
  - genesis2=*=mpi_openmpi*
  # NOTE: be sure to update ./env-genesis-only.yml as well.
  - genesis4 >=4.6.13=mpi_openmpi*
  - h5py=*=mpi_openmpi*
  - openmpi
  # Required for all codebase features:
  - ffmpeg # genesis2 example movie
  - pytao # from_tao
  - ruff # dataclass regeneration
  - jinja2 # dataclass regeneration
  # Running the test suite
  - pytest
  - pytest-cov
  # Notebooks
  - jupyterlab >=3
  # - pygments
  # Documentation building
  - mkdocs
  - mkdocstrings
  - mkdocs-jupyter
  - mkdocs-material
  - mkdocstrings-python
  - pip
